Regulus Backend

This package provides the Regulus FastAPI backend.

- Run: `uvicorn app.main:app --host 0.0.0.0 --port 8000`
- Endpoints: `/health`, `/metrics`, `/upload`, `/query`, `/hybrid_status`
