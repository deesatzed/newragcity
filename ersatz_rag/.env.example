# ERSATZ_RAG Environment Configuration Template
# Copy this file to appropriate subdirectories and customize

# =============================================================================
# GLOBAL CONFIGURATION
# =============================================================================

# LLM API Keys (at least one required for all systems)
OPENAI_API_KEY=your_openai_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here

# PageIndex Document Processing
PAGEINDEX_API_KEY=your_pageindex_api_key_here

# =============================================================================
# REGULUS CONFIGURATION (Enterprise Compliance Platform)
# =============================================================================
# Copy to regulus/.env for Regulus deployment

# Database Configuration
DATABASE_URL=postgresql://regulus_user:secure_password@localhost:5432/regulus
POSTGRES_USER=regulus_user
POSTGRES_PASSWORD=secure_password
POSTGRES_DB=regulus

# Redis for Background Jobs
REDIS_URL=redis://localhost:6379

# Application Settings
DEBUG=False
SECRET_KEY=your_secret_key_minimum_32_characters
CONFIDENCE_THRESHOLD=0.80

# =============================================================================
# COGNITRON CONFIGURATION (Medical-Grade Knowledge Assistant)
# =============================================================================
# Copy to cognitron/.env for Cognitron deployment

# Medical-Grade Thresholds
COGNITRON_CRITICAL_THRESHOLD=0.95
COGNITRON_PRODUCTION_THRESHOLD=0.85
COGNITRON_DISPLAY_THRESHOLD=0.70

# Storage Paths (Optional - defaults to ~/.cognitron/)
COGNITRON_INDEX_PATH=~/.cognitron/index
COGNITRON_MEMORY_PATH=~/.cognitron/memory.db

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Performance
MAX_WORKERS=4
REQUEST_TIMEOUT=30

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. For Regulus: Copy relevant sections to regulus/.env
# 2. For Cognitron: Copy relevant sections to cognitron/.env
# 3. Replace all placeholder values with actual credentials
# 4. Keep .env files secure and never commit them to version control
